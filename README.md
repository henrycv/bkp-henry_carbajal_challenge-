# henry_carbajal_challenge


# NOTE
For Laravel installation on PHP docker container, sure to add '$HOME/.composer/vendor/bin' to $PATH environment variable:
<code>export PATH="$HOME/.composer/vendor/bin:$PATH"</code>